# .gitignore (Git — Ignore List)

# scratch space for fresh Claude Design exports before extracting into site/
_reference/
_references/

# local planning notes, not for the repo history
.plan/

# workflow build output (generated in CI, never committed)
build/

# npm deps for scripts/ssr-render.js (build-time only, never shipped)
node_modules/

# uv-managed venv for ruff (dev tool only, never shipped)
.venv/

# auto-generated thumbnail cache
.thumbnail

# Python bytecode cache
__pycache__/
*.pyc

site/github.md
site/CLAUDE.md
site/.claude/
site/uploads/
site/screenshots/
site/*.png
